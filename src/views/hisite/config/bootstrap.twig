<?php

if (!defined('HISITE_ROOT_DIR')) {
    define('HISITE_ROOT_DIR', dirname(dirname(__DIR__)));
}

$bootstrap = HISITE_ROOT_DIR . '/vendor/hiqdev/hisite-core/src/config/bootstrap.php';

if (!file_exists($bootstrap)) {
    fwrite(STDERR, "Run composer to set up dependencies!\n");
    exit(1);
}

require_once $bootstrap;
