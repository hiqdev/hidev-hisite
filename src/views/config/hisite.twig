<?php

$common = require __DIR__ . '/common.php';

$config = [
    'components' => [
        'request' => [
            'cookieValidationKey' => $common['params']['cookieValidationKey'],
        ],
    ],
];

return yii\helpers\ArrayHelper::merge($common, $config);
